const Player = (token) => {
  this.token = token;

  const getToken = () => {
    return token;
  };

  return {getToken};
};

const gameBoard = (() => {

})();

const displayController = (() => {

})();

const gameController = (() => {

})();